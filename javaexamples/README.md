This directory declares a number of simple Java binaries which use different
Java language/platform features. They are executed during the PhasicJ
TraceLogger's integration tests to check whether that tool behaves correctly
when faced these various language features.
